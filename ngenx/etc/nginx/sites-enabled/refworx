server {
       listen 81;
       listen [::]:81;

       server_name refworx.leica.com;

       root /var/www/refworx;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}